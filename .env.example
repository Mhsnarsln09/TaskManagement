# Bu dosya yalnızca gerekli değişkenleri belgeler; gerçek parola içermez.
POSTGRES_DB=taskmanagement
POSTGRES_USER=taskmanagement
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432
API_PORT=8080
JWT_SIGNING_KEY=replace-with-at-least-32-random-bytes

# ASP.NET Core configuration key format for local development.
# Replace change-me with your local password; do not commit real secrets.
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=taskmanagement;Username=taskmanagement;Password=change-me
